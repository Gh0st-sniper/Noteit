<h1> <%= @note.title %> </h1>
<p> <%= @note.description %> </p>
<% if @note.tag %>
<p><i><u><%= @note.tag %></u></i></p>

<% end %>

<%= link_to 'Delete', note_path(@note), data: { turbo_method: :delete , turbo_confirm: "Are you sure?"} %>
<%= link_to 'Edit', edit_note_path %>
<%= link_to 'Home', root_path %>
